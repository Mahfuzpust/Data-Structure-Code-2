#include<stdio.h>
int array[8];
int Maxindex=8,index=-1;
int is_full()
{
    if(index==Maxindex-1)return 1;
    else return 0;
}
void push(int value)
{
    array[index++]=value;
}

int is_empty()
{
    if(index=-1)return 1;
    else return 0;
}
void pop()
{
    --index;
}



int main()
{
    int i;
    while(!is_full())
    {
        int input;
        scanf("%d",&input);
        push(input);
    }
    for(i=0;i<index;i++)printf("%d ",array[i]);

    while(!is_empty)
    {
       
    }
}
